import React from 'react'
import { Box, Card, CardHeader,  } from '@material-ui/core'

const Flights = ({ data }) => {
  const { destination, startDate, endDate, latitude, longitude } = data
  return (
    <div>
      
    </div>
  )
}

export default Flights
